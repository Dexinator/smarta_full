---
// Página de Oficina Virtual de Turismo
// Ahora usa VirtualOfficeLayout con contenido desde content collections
import { getEntry } from 'astro:content';
import VirtualOfficeLayout from '../layouts/VirtualOfficeLayout.astro';

// Cargar configuración desde content collection
const config = await getEntry('virtual-offices', 'oficina-turismo');
---

<VirtualOfficeLayout config={config.data} />
